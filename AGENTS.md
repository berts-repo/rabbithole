# Agent Entry

Start with `CONTEXT.md` for project context, source-of-truth rules, hard
constraints, and task-specific read orders.

For current implementation work, read `docs/work/ACTIVE.md`.

For documentation management, read `LIBRARIAN.md`.
