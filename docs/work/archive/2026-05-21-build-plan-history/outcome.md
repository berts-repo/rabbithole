# Outcome

The former `docs/specs/PLAN.md` was moved to this archived work package as
`plan.md`.

`docs/specs/PLAN.md` is now a small compatibility pointer that directs readers
to current reference docs, active work, and the archived historical plan.

This preserves old links while removing the phase-status work object from the
spec source-material directory.
