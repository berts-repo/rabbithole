# Handoff

Status: implemented.

Next package: `../2026-05-21-crawler-privacy-cleanup/`.

Notes:

- The current app truth remains in `docs/reference/`.
- The original prompt/spec corpus is now in `docs/specs/`.
- Historical phase docs are now in `docs/work/archive/`.
- Future work should create or update a work package instead of adding loose
  root-level plans.
