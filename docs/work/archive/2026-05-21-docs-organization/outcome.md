# Outcome

The documentation tree now separates current truth, source specs, active work,
and archive history. Agent entrypoints point to `docs/reference/` and
`docs/work/ACTIVE.md`.

The next owner-confirmed work is crawler privacy cleanup.
